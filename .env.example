# RateWalk environment. Copy to .env and fill in.
# Get a free FRED API key at https://fredaccount.stlouisfed.org/apikeys
# With this set, `source: auto` (the default) pulls real Treasury/CPI data;
# without it, RateWalk falls back to the deterministic synthetic generator.
FRED_API_KEY=
